apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lost-and-found-ingress
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lost-and-found-api-service
            port:
              number: 80
